# Application-specific values for the shared chart:
# https://github.com/bcgov/nr-vault-agent-integration
vaultAgent:
  vaultAddress: https://knox.io.nrs.gov.bc.ca
  approleMountPath: auth/vs_apps_approle
  loginSecretName: knox-audit-secret
  pvcSize: 64Mi

  secrets:
    - name: service
      secretPath: dev/data/project/service/secrets
      keys:
        - db_username
        - db_password
      format: env
      outputFile: /vault/output/service.env

  # Keep the chart's resource defaults unless this application needs overrides.
  resources:
    requests:
      cpu: 10m
      memory: 32Mi
    limits:
      memory: 128Mi
